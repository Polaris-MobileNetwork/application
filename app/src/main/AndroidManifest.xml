<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!--
        Declare any permissions your app needs here.
        For a network test drive app, you'll eventually need:
        <uses-permission android:name="android.permission.INTERNET" />
        <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
        <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" /> (for cell tower info & GPS)
        <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" /> (for cell tower info & network location)
        <uses-permission android:name="android.permission.READ_PHONE_STATE" /> (for some telephony info, be mindful of privacy)
        If targeting Android 10 (API 29) or higher and need background location for continuous network scanning:
        <uses-permission android:name="android.permission.ACCESS_BACKGROUND_LOCATION" />
        If using Foreground Services for continuous collection (Android 9+):
        <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
        If targeting Android 13 (API 33) and using ForegroundService for "phoneCall" or "location":
        <uses-permission android:name="android.permission.FOREGROUND_SERVICE_LOCATION" />
        <uses-permission android:name="android.permission.FOREGROUND_SERVICE_CONNECTED_DEVICE" /> (Example, if interacting with BT/Wi-Fi devices)
    -->

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Polaris"
        tools:targetApi="31">

        <activity
            android:name=".MainActivity"  android:exported="true"
            android:label="@string/app_name"
            android:windowSoftInputMode="adjustResize" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

    </application>

</manifest>
